import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#28282B',
    },
    secondary: {
      main: '#E2DFD2',
    },
    error: {
      main: "#ff1744",
    },
  },
});

export default theme;
